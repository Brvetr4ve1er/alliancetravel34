[build]
  # No build command — the site/ directory is pre-built static HTML/CSS/JS.
  publish = "site"

[build.environment]
  NODE_VERSION = "22"

# 404 fallback — serve the custom 404 page for any unmatched path.
[[redirects]]
  from   = "/*"
  to     = "/404.html"
  status = 404
